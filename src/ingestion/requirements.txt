# Dataflow Pipeline Dependencies
# 
# SINGLE SOURCE OF TRUTH: All dependencies are defined in setup.py
# This file exists only for compatibility with tools that expect requirements.txt
#
# To install dependencies, use one of:
#   pip install -e .          # Install from setup.py (recommended)
#   pip install -r requirements.txt  # Install via this file (legacy)

# Install the iot-telemetry-pipeline package and all dependencies
-e .